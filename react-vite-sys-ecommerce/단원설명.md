### 1. @ 사용할 수 있도록
### @를 /src 디렉토리로 바꿔서 쓸 수 있게 하겠다.

export default defineConfig({
  plugins: [react()],
  resolve:{
    alies:[
      {find: '@', replacement:path.resolve(__dirname, 'src')}
    ]
  }
})


### 2. 아래를 package.json에 추가
    "bootstrap": "^5.3.3",
    "styled-components": "^6.1.12",
    "react-router-dom": "^6.26.0"

### 3. .prettierrc 프리티어 설정
{
    "singleQuote": true,
    "semi": true,
    "tabWidth": 4,
    "trailingComma": "all",
    "printWidth": 120
}

## 탬플릿을 쓰기 위한 작업
### 1. fruits 폴더 생성 후 탬플릿을 assets/fruit에 넣음
### 2. index.html 에 라이브러리 추가 및 정리
### 3. 라우터 수정